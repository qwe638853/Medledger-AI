# Ollama 配置
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=meditron:7b

# ChromaDB 路徑（Linux/WSL 環境）
CHROMA_DB_PATH=./chroma_db

# Python Backend gRPC 配置
PYTHON_BACKEND_GRPC_PORT=50052
PYTHON_BACKEND_GRPC_HOST=[::]

# 如果使用 Windows 路徑（不推薦，僅在 Windows 環境下使用）
# CHROMA_DB_PATH=D:/gg/chroma_db
